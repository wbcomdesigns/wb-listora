{
  "mode": "combo",
  "release_version": "1.5.0",
  "pro_version": "1.5.0",
  "ran_at": "2026-08-11T21:33:39Z",
  "gates": {
    "contract_audit": "not_run",
    "architecture_invariants": "pass",
    "local_ci": "not_run"
  },
  "sections": {
    "A_fresh_install": { "pass": 4, "fail": 0, "skipped": 4 },
    "B_upgrade": { "pass": 0, "fail": 0, "skipped": 4 },
    "C_core_flows": { "pass": 13, "fail": 0, "skipped": 28 },
    "D_regression_guards": { "pass": 4, "fail": 0, "skipped": 108 },
    "E_pro_smoke": { "pass": 4, "fail": 0, "skipped": 34 },
    "F_cross_browser": { "pass": 1, "fail": 0, "skipped": 4 },
    "S_pro_supplements": { "pass": 4, "fail": 0, "skipped": 20 }
  },
  "core_rows": {
    "total": 16,
    "executed": 15,
    "not_executed": [
      "A1 - Activation and first-request routing (would require wp plugin deactivate/activate mid-walk; skipped to avoid destabilizing the combo state within a verification-only session)"
    ]
  },
  "reverified_fixes": [
    {
      "id": "map-grid-agree",
      "result": "pass",
      "evidence": "Directory page IAPI state (mapConfig.markers) checked against DB truth for all 3 fixtures. (1) ?type=hotel&keyword=zzzznomatch: grid .listora-card--empty computed-visible (display:block/visibility:visible) AND mapConfig.markers.length === 0. (2) ?type=hotel: mapConfig.markers.length === 20, every marker.type === 'hotel', matches SELECT COUNT(*) FROM wp_listora_search_index WHERE status='publish' AND listing_type='hotel' AND lat!=0 = 20. (3) Unfiltered /listings/: mapConfig.markers.length === 99, matches the geo-join total (SELECT COUNT(*) FROM search_index JOIN geo WHERE status='publish' = 99), well under map_max_markers=500 so no capping in play."
    },
    {
      "id": "favorite-count-live",
      "result": "pass",
      "evidence": "Tested both arrival states on listing detail as logged-in member qa_vendor_01. Non-zero (The Golden Fork, id 17, started at 1 favorite from another user): .listora-detail__favorite-count starts computed-visible reading '1', toggle-on -> '2' with no reload (verified DB COUNT(*)=2 matched), toggle-off -> restored '1' (DB COUNT(*)=1). Zero fixture (Sakura House, id 24): node present but display:none at count 0, toggle-on -> becomes computed-visible (display:block) reading '1' (DB COUNT(*)=1), toggle-off -> hidden again (DB COUNT(*)=0). All fixture state restored to original after test."
    },
    {
      "id": "webhook-review-rating",
      "result": "pass",
      "evidence": "Submitted a real 4-star review (qa_vendor_01 on Le Petit Bistro, id 45) via the browser review form -> DB row overall_rating=4. Invoked Outgoing_Webhooks::build_review_payload(129,45,6) via reflection: returned rating:4, exactly matching the DB overall_rating column (not 0). grep of debug.log diff for 'Undefined array key' after the submission: zero hits. Source confirms the fix is in place at class-outgoing-webhooks.php:555 (reads $review['overall_rating'], comment documents the prior bug). Test review deleted afterward to restore fixture state."
    }
  ],
  "failures": [
    {
      "id": "runbook-fixture-cleanup-wrong-columns",
      "origin": "for",
      "triage_note": "Not a customer-facing defect - this SQL only runs as part of the QA runbook's own 'Fixture cleanup (before every walk)' snippet, never in any production code path. Filed so the runbook doc gets corrected; does not block release.",
      "expected": "DELETE FROM wp_listora_reviews WHERE comment_content LIKE \"Smoke %\" and DELETE FROM wp_listora_claims WHERE message LIKE \"Smoke %\" execute cleanly (0 rows affected on a clean fixture set)",
      "actual": "Both statements throw 'WordPress database error: Unknown column' - wp_listora_reviews has no comment_content column (the real column is content) and wp_listora_claims has no message column (the real column is proof_text). Confirmed via DESCRIBE on both tables. AGENT_SMOKE_RUNBOOK.md lines 97-109 need the column names corrected.",
      "url": "n/a (wp-cli eval, not a page)",
      "evidence": "wp-content/plugins/wb-listora/docs/qa/AGENT_SMOKE_RUNBOOK.md:103-105; DESCRIBE wp_listora_reviews shows column `content`; DESCRIBE wp_listora_claims shows column `proof_text`"
    }
  ],
  "debug_log_issues": [
    {
      "section": "fixture-cleanup",
      "level": "db_error",
      "line": "WordPress database error Unknown column 'comment_content' in 'where clause' for query DELETE FROM wp_listora_reviews WHERE comment_content LIKE \"Smoke %\"",
      "file": "docs/qa/AGENT_SMOKE_RUNBOOK.md fixture-cleanup snippet (not plugin code)"
    },
    {
      "section": "fixture-cleanup",
      "level": "db_error",
      "line": "WordPress database error Unknown column 'message' in 'where clause' for query DELETE FROM wp_listora_claims WHERE message LIKE \"Smoke %\"",
      "file": "docs/qa/AGENT_SMOKE_RUNBOOK.md fixture-cleanup snippet (not plugin code)"
    },
    {
      "section": "cross-cutting (WP core noise)",
      "level": "deprecation",
      "line": "PHP Deprecated: preg_replace(): Passing null to parameter #3 ($subject) of type array|string is deprecated (12 occurrences across the walk, one per pageload that triggers kses filtering)",
      "file": "wp-includes/kses.php:1939 (WordPress core, PHP 8.4 compat gap - pre-existing, not caused by wb-listora/wb-listora-pro per CLAUDE.md's documented exception)"
    },
    {
      "section": "ad-hoc credit top-up (self-inflicted test-harness error)",
      "level": "fatal",
      "line": "PHP Fatal error: Uncaught TypeError: Wbcom\\Credits\\Credits::adjust(): Argument #2 ($user_id) must be of type int, string given - thrown from an ad-hoc `wp eval` snippet this smoke agent wrote with the wrong argument order/count while granting a test fixture credits. Immediately corrected to use Credits::topup() with the right signature; no customer-facing code path was involved and no plugin file was implicated as the root cause.",
      "file": "wp-content/plugins/wb-listora/libs/wbcom-credits-sdk/src/Credits.php:317 (call site was this agent's WP-CLI eval command, not any plugin/theme code)"
    }
  ],
  "manual_required": [
    "Pro's CLAUDE.md README table (E.pro-cross-coupling / A3 contract) still lists `wp_listora_saved_searches` as a Pro-created table; Pro_Migrator (DB_VERSION 1.14.0) shows it was retired in 1.13.0 and folded into `_listora_saved_searches` user meta. Table correctly absent on this install - functionally correct, just stale prose in CLAUDE.md worth a doc fix.",
    "C.admin.settings-each-tab was verified for clean render (14 tabs, 0 error notices, 0 console errors) but per-tab Save was only exercised indirectly; a full per-tab save round-trip (C.admin.settings-merge) was not run this walk.",
    "F.firefox-desktop, F.safari-ios, F.rtl, F.a11y not executed - single Chromium session only, per the runbook's session budget guidance.",
    "S26 (app-nonce-exempt writes for the native app) was spot-checked for the anonymous-no-nonce corner (403, correct) but the authenticated-Application-Password corner was not exercised this walk.",
    "108 of 112 D-table regression-guard rows and 34 of 38 E-section Pro toggle rows were not reached; this was a budget-prioritized CORE-first walk per the runbook's own guidance, not a claim of full coverage."
  ]
}
