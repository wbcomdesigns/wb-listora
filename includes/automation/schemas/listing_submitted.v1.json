{
	"$schema": "https://json-schema.org/draft-07/schema#",
	"title": "listing_submitted",
	"description": "A member submitted a new listing. Fires whether the submission resolved to publish (auto-approve) or pending (manual moderation).",
	"type": "object",
	"required": ["listing", "status"],
	"properties": {
		"listing": {
			"type": "object",
			"description": "Canonical listing card, identical to what GET /listings returns.",
			"required": ["id", "title", "link"],
			"properties": {
				"id": { "type": "integer" },
				"title": { "type": "string" },
				"link": { "type": "string", "format": "uri" },
				"excerpt": { "type": "string" },
				"featured_image": { "type": ["object", "null"] },
				"rating": { "type": "object" }
			}
		},
		"status": {
			"type": "string",
			"description": "The status the listing was created in: publish or pending."
		}
	}
}
