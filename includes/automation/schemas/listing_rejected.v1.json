{
	"$schema": "https://json-schema.org/draft-07/schema#",
	"title": "listing_rejected",
	"description": "A listing moved into the rejected status from pending or publish.",
	"type": "object",
	"required": ["listing"],
	"properties": {
		"listing": {
			"type": "object",
			"description": "Canonical listing card, identical to what GET /listings returns.",
			"required": ["id", "title", "link"],
			"properties": {
				"id": { "type": "integer" },
				"title": { "type": "string" },
				"link": { "type": "string", "format": "uri" },
				"excerpt": { "type": "string" },
				"featured_image": { "type": ["object", "null"] },
				"rating": { "type": "object" }
			}
		},
		"previous_status": {
			"type": "string",
			"description": "The status the listing held before rejection."
		}
	}
}
