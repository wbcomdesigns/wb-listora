{
	"$schema": "https://json-schema.org/draft-07/schema#",
	"title": "claim_submitted",
	"description": "A member submitted a claim requesting ownership of a listing. The payload carries the claimant's contact details, so subscribing to this event requires the same capability as configuring webhooks.",
	"type": "object",
	"required": ["claim"],
	"properties": {
		"claim": {
			"type": "object",
			"description": "Canonical claim, identical to what GET /claims returns per row.",
			"required": ["id", "listing_id", "user_id", "status"],
			"properties": {
				"id": { "type": "integer" },
				"listing_id": { "type": "integer" },
				"listing_title": { "type": "string" },
				"listing_url": { "type": "string", "format": "uri" },
				"user_id": { "type": "integer" },
				"user_name": { "type": "string" },
				"user_email": { "type": "string" },
				"status": { "type": "string" },
				"proof_text": { "type": "string" },
				"created_at": { "type": "string" }
			}
		}
	}
}
